<resources>
    <!-- Default screen margins, per the Android Design guidelines. -->
    <dimen name="activity_horizontal_margin">16dp</dimen>
    <dimen name="activity_vertical_margin">16dp</dimen>
    <dimen name="fab_margin">16dp</dimen>
    <dimen name="app_bar_height">180dp</dimen>
    <dimen name="text_margin">16dp</dimen>

    <!-- Text Sizes-->
    <dimen name="text_size_xsmall">10sp</dimen>
    <dimen name="text_size_small">12sp</dimen>
    <dimen name="text_size_medium">18sp</dimen>
    <dimen name="text_size_large">24sp</dimen>
    <dimen name="text_size_title">40sp</dimen>
    <dimen name="text_size_subtitle">18sp</dimen>

    <!-- Size of the score dialog -->
    <dimen name="score_dialog_width">320dp</dimen>
    <dimen name="score_dialog_height">400dp</dimen>

    <!-- Size of the options dialog -->
    <dimen name="options_dialog_width">320dp</dimen>
    <dimen name="options_dialog_height">320dp</dimen>

    <!-- Size of the Score Display -->
    <dimen name="score_display_width">80dp</dimen>

    <!-- Height of the score progress history graph -->
    <dimen name="chord_progress_history_height">64dp</dimen>

    <!-- Minimum size of the chord dropdown menu -->
    <dimen name="min_chord_dropdown_menu_size">240dp</dimen>

    <!-- Minimum size of the chord select spinner -->
    <dimen name="min_chord_select_slider_size">200dp</dimen>

    <!-- Minimum width of the instrument dropdown menu-->
    <dimen name="min_instrument_dropdown_menu_width">240dp</dimen>

    <!-- Height of the instrument icon images in the instrument select spinner -->
    <dimen name="instrument_icon_height">48dp</dimen>

</resources>
